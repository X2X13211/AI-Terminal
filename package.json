{
  "name": "ai-terminal",
  "version": "1.0.0",
  "main": "terminal.js",
  "scripts": {
    "start": "node terminal.js",
    "build": "pkg terminal.js --target node18-win-x64 --output ai-terminal.exe"
  },
  "dependencies": {
    "node-persist": "^4.0.0"
  },
  "pkg": {
    "assets": [
      "config.js",
      "chat-storage/**/*"
    ]
  }
}